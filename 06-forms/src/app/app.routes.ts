import { Routes } from '@angular/router';
import { ReactiveFormsRootComponent } from './01-reactive-forms/reactive-forms-root/reactive-forms-root.component';

export const routes: Routes = [
    {
        path: '01',
        component: ReactiveFormsRootComponent
    }
];
